---
// HORA POR DEFINIR — default 7:00 p.m., update when set
const date = new Date('2026-10-03T19:00:00-05:00');
const mapEmbed =
  'https://maps.google.com/maps?q=' +
  encodeURIComponent('Av. 33 #74e-116, Laureles - Estadio, Medellín, Antioquia, Colombia') +
  '&z=16&output=embed';
const mapUrl =
  'https://www.google.com/maps/search/?api=1&query=' +
  encodeURIComponent('Av. 33 #74e-116, Laureles - Estadio, Medellín, Antioquia, Colombia');
const message = encodeURIComponent('Hola, confirmo mi asistencia a los XV años de Susana.');
const phone = '573007329023';
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="theme-color" content="#4C2D1F" />
    <title>Mis XV · Susana</title>
  </head>
  <body>
    <main>
      <section class="cover">
        <div class="cover-art" aria-hidden="true"></div>

        <div class="cover-copy">
          <p class="eyebrow">Con mucho cariño</p>
          <p class="small-title">Mis</p>
          <h1>XV</h1>
          <p class="name">Susana</p>
          <p class="date">03 · 10 · 2026</p>
          <a class="scroll-arrow" href="#historia" aria-label="Continuar a la invitación">↓</a>
        </div>
      </section>

      <section class="story section" id="historia">
        <p class="eyebrow">Una nueva etapa</p>

        <div class="invite">
          <p class="invite-family">Marisela Restrepo Mona<br />y Andrés Garcés Quintero</p>
          <p class="invite-cta">te invitan a celebrar los XV años de</p>
          <p class="invite-hero">Susana</p>
        </div>

        <h2 class="story-heading">Hay alegrías que<br />se disfrutan más<br /><em>cuando se comparten.</em></h2>

        <div class="copy invite-copy">
          <p>Quince años que se cumplen con alegría y una noche que quiero compartir contigo. Gracias por ser parte de esta historia.</p>
          <p>Hay historias y personas que no voy a olvidar y recuerdos como este que siempre voy a guardar.</p>
          <p>Porque son parte de mi vida es mi deseo que compartan conmigo este día tan especial.</p>
          <p>Te espero con mucha ilusión para celebrar juntos.</p>
          <p class="signature">Con cariño,<br /><strong>Susana</strong></p>
        </div>
      </section>

      <section class="event section">
        <p class="eyebrow">Guarda la fecha</p>
        <div class="event-card">
          <div><span class="label">Sábado</span><strong class="date-display"><b>3</b><span>octubre<br /><small>2026</small></span></strong></div>
          <div><span class="label">Recepción</span><strong>7:00<br /><small>p. m.</small></strong></div>
        </div>
        <div class="countdown" data-countdown data-target={date.toISOString()}><div><b data-days>00</b><span>días</span></div><div><b data-hours>00</b><span>horas</span></div><div><b data-minutes>00</b><span>minutos</span></div><div><b data-seconds>00</b><span>segundos</span></div></div>
        <p class="punctual">Te esperamos con puntualidad para disfrutar desde el inicio de la fiesta y compartir más tiempo juntos.</p>
      </section>

      <section class="venue section">
        <div class="venue-photo"><iframe title="Ubicación de la recepción" src={mapEmbed} style="border:0" allowfullscreen loading="lazy" referrerpolicy="strict-origin-when-cross-origin"></iframe></div>
        <div class="venue-copy">
          <p class="eyebrow">El lugar</p>
          <h2>Laureles · Estadio<br /><em>Medellín</em></h2>
          <address>Av. 33 # 74 e-116<br />Laureles - Estadio · Medellín, Antioquia</address>
          <a class="button" href={mapUrl} target="_blank" rel="noreferrer">Cómo llegar <span>↗</span></a>
        </div>
      </section>

      <section class="details section">
        <p class="eyebrow">Detalles de la noche</p>
        <h2 class="details-title">Todo pensado para<br /><em>celebrar juntos.</em></h2>
        <div class="detail-grid">
          <article><span class="detail-symbol">✦</span><h3>Código de<br /><em>vestimenta</em></h3><p><strong>Tipo cóctel</strong><br />Se permiten <span class="earth-word">colores tierra</span>, blanco y negro.</p></article>
          <article><span class="detail-symbol">♡</span><h3>Mesa de<br /><em>regalos</em></h3><p><strong>Lluvia de sobres</strong><br />Tu presencia será nuestro regalo más bonito.</p></article>
        </div>
      </section>

      <section class="songs section" id="canciones">
        <p class="eyebrow">La playlist</p>
        <h2>¿Qué canción<br /><em>no puede faltar?</em></h2>
        <p class="songs-intro">Ayúdame a armar la pista de baile. Sugiere esa canción que te hace levantar de la silla y deja un comentario si quieres dedicarla.</p>

        <form id="song-form" class="song-form" novalidate>
          <div class="song-grid">
            <label class="field"><span>Tu nombre <i>(opcional)</i></span><input id="song-name" type="text" maxlength="40" placeholder="Ej. Valentina" autocomplete="name" /></label>
            <label class="field"><span>Canción <i>*</i></span><input id="song-title" type="text" required maxlength="80" placeholder="Ej. Día de Enero" /></label>
            <label class="field"><span>Artista</span><input id="song-artist" type="text" maxlength="80" placeholder="Ej. Shakira" /></label>
          </div>
          <label class="field field-full"><span>Comentario / dedicatoria <i>(opcional)</i></span><textarea id="song-comment" rows="2" maxlength="140" placeholder="Ej. ¡Esta no puede faltar para bailar todos juntos!"></textarea></label>
          <div class="song-actions">
            <button type="submit" class="button" id="song-submit">Sugerir canción ♪</button>
            <p class="song-hint" id="song-hint" aria-live="polite"></p>
          </div>
        </form>

        <div class="song-wall-wrap">
          <div class="song-wall-head"><h3>Canciones sugeridas</h3><span id="song-count" class="song-count">0</span></div>
          <ul id="song-wall" class="song-wall" aria-live="polite"></ul>
          <p id="song-empty" class="song-empty">¡Sé quien estrene la playlist! Tu canción la verán todos los invitados aquí.</p>
        </div>
      </section>

      <section class="rsvp section">
        <p class="eyebrow">Confirma tu asistencia</p>
        <h2>¿Nos<br /><em>acompañas?</em></h2>
        <p>Por favor confirma antes del <strong>20 de septiembre de 2026</strong>.</p>

        <div class="actions"><a class="button button-light" href={`https://wa.me/${phone}?text=${message}`} target="_blank" rel="noreferrer">Confirmar por WhatsApp ↗</a></div>
      </section>

      <footer>
        <p class="hashtag">#Mis15Susana</p>
        <p class="footer-name">Susana · XV</p>
      </footer>
    </main>

    <!-- Música: Barbie 12 Princesas — FAB fijo, no autoplay -->
    <audio id="bg-audio" src="/barbie-12-princesas.mp3" preload="none" loop></audio>
    <button id="music-fab" class="music-fab" type="button" aria-label="Reproducir música" aria-pressed="false" title="Música">
      <span class="music-icon" aria-hidden="true">♫</span><span class="music-text">Música</span>
    </button>

    <script is:inline>
      const countdown = document.querySelector('[data-countdown]');
      const tick = () => { const diff = Math.max(0, new Date(countdown.dataset.target).getTime() - Date.now()); const values = [Math.floor(diff / 86400000), Math.floor(diff / 3600000) % 24, Math.floor(diff / 60000) % 60, Math.floor(diff / 1000) % 60]; ['days','hours','minutes','seconds'].forEach((key, i) => document.querySelector(`[data-${key}]`).textContent = String(values[i]).padStart(2, '0')); };
      tick(); setInterval(tick, 1000);
    </script>
    <script is:inline>
      // ── Música FAB: sin autoplay (políticas de navegadores) — solo por tap ──
      (function(){
        const audio = document.getElementById('bg-audio');
        const btn = document.getElementById('music-fab');
        if(!audio || !btn) return;
        const icon = btn.querySelector('.music-icon');
        const text = btn.querySelector('.music-text');
        let playing = false;
        const setState = (on) => {
          playing = on;
          btn.setAttribute('aria-pressed', String(on));
          btn.setAttribute('aria-label', on ? 'Pausar música' : 'Reproducir música');
          if(icon) icon.textContent = on ? '❚❚' : '♫';
          if(text) text.textContent = on ? 'Pausar' : 'Música';
        };
        btn.addEventListener('click', async () => {
          try {
            if(!playing){ audio.volume = 0.7; await audio.play(); setState(true); }
            else { audio.pause(); setState(false); }
          } catch(e){
            console.warn('[audio] play failed', e);
            setState(false);
          }
        });
        audio.addEventListener('ended', () => setState(false));
        audio.addEventListener('error', () => { btn.style.display='none'; });
      })();
    </script>
    <script is:inline>
      // ── Muro de canciones: PÚBLICO y COMPARTIDO ──
      // Modo público: todos los invitados ven las mismas canciones (Supabase).
      // Fallback localStorage si la tabla aún no existe.
      (function(){
        const STORAGE_KEY = 'susana-songs-v1';
        const SUPABASE_URL = 'https://pfzhazkcyarlwazwziec.supabase.co';
        const SUPABASE_ANON = 'sb_publishable_mxYQbSZsvviLuSBm1-fUaQ_U4iAFIQ-';
        const TABLE = 'susana_songs';
        const form = document.getElementById('song-form');
        const $name = document.getElementById('song-name');
        const $title = document.getElementById('song-title');
        const $artist = document.getElementById('song-artist');
        const $comment = document.getElementById('song-comment');
        const $wall = document.getElementById('song-wall');
        const $empty = document.getElementById('song-empty');
        const $count = document.getElementById('song-count');
        const $hint = document.getElementById('song-hint');
        if(!form || !$wall) return;

        const esc = (s) => String(s).replace(/[&<>"']/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
        const loadLocal = () => { try { return JSON.parse(localStorage.getItem(STORAGE_KEY)||'[]'); } catch { return []; } };
        const saveLocal = (arr) => localStorage.setItem(STORAGE_KEY, JSON.stringify(arr));

        const SEEDS = [];

        let remoteItems = null; // null = aún no probado, [] = tabla vacía, array = datos remotos
        let useRemote = false;

        function toCard(it){
          // normaliza tanto formato local como remoto
          return {
            name: it.guest_name ?? it.name ?? '',
            title: it.song ?? it.title ?? '',
            artist: it.artist ?? '',
            comment: it.comment ?? '',
            at: it.created_at ? new Date(it.created_at).getTime() : (it.at || Date.now())
          };
        }

        async function fetchRemote(){
          try {
            const r = await fetch(`${SUPABASE_URL}/rest/v1/${TABLE}?select=*&order=created_at.desc`, {
              headers: { apikey: SUPABASE_ANON, Authorization: 'Bearer '+SUPABASE_ANON }
            });
            if(!r.ok) throw new Error(r.status+' '+await r.text());
            const data = await r.json();
            remoteItems = data;
            useRemote = true;
            return data;
          } catch(err){
            console.warn('[songs] Supabase no disponible, usando localStorage', err);
            remoteItems = null;
            useRemote = false;
            return null;
          }
        }

        async function insertRemote(it){
          try {
            const r = await fetch(`${SUPABASE_URL}/rest/v1/${TABLE}`, {
              method: 'POST',
              headers: { apikey: SUPABASE_ANON, Authorization: 'Bearer '+SUPABASE_ANON, 'Content-Type':'application/json', Prefer:'return=representation' },
              body: JSON.stringify({ guest_name: it.name || null, song: it.title, artist: it.artist || null, comment: it.comment || null })
            });
            if(!r.ok) throw new Error(await r.text());
            return await r.json();
          } catch(err){
            console.warn('[songs] insert remoto falló', err);
            return null;
          }
        }

        function render(){
          const source = useRemote ? remoteItems : null;
          let items;
          if(source !== null){
            items = source.map(toCard);
          } else {
            items = loadLocal().map(toCard);
          }

          if(!items.length){
            $wall.innerHTML = '';
            $count.textContent = '0';
            $count.title = useRemote ? '0 canciones públicas' : '0 en este dispositivo';
            $empty.style.display = 'block';
            $empty.textContent = '¡Sé quien estrene la playlist! Tu canción la verán todos los invitados aquí.';
            return;
          }
          $wall.innerHTML = items.map((it, idx) => {
            return `<li class="song-card" data-idx="${idx}">
              <div class="song-card-head">
                <div>
                  <p class="song-card-title">${esc(it.title)}${it.artist ? ` <span style="font-weight:400;color:#6B564A">— ${esc(it.artist)}</span>` : ''}</p>
                  <p class="song-card-artist">Sugirió: <strong>${esc(it.name || 'Invitado anónimo')}</strong></p>
                </div>
                <span class="song-card-meta">♪</span>
              </div>
              ${it.comment ? `<p class="song-card-comment">“${esc(it.comment)}”</p>` : ''}${!useRemote ? `<div class="song-card-actions"><button type="button" data-del="${idx}" aria-label="Eliminar sugerencia">Quitar</button></div>` : ''}
            </li>`;
          }).join('');
          $count.textContent = String(items.length);
          $count.title = useRemote ? `${items.length} canciones públicas` : `${items.length} en este dispositivo`;
          $empty.style.display = 'none';
        }

        $wall.addEventListener('click', (e) => {
          const del = e.target.closest('[data-del]');
          if(del && !useRemote){
            const idx = Number(del.getAttribute('data-del'));
            const items = loadLocal();
            items.splice(idx,1);
            saveLocal(items);
            $hint.textContent = 'Sugerencia eliminada de este dispositivo.';
            $hint.className = 'song-hint ok';
            render();
          }
        });

        form.addEventListener('submit', async (e) => {
          e.preventDefault();
          const title = $title.value.trim();
          const artist = $artist.value.trim();
          const name = $name.value.trim();
          const comment = $comment.value.trim();
          if(!title){
            $hint.textContent = 'Escribe al menos el nombre de la canción ♪';
            $hint.className = 'song-hint error';
            $title.focus();
            return;
          }
          if(title.length < 2){
            $hint.textContent = 'La canción es muy corta, revisa el nombre.';
            $hint.className = 'song-hint error';
            return;
          }
          const it = { name: name || '', title, artist: artist || '', comment: comment || '', at: Date.now() };

          // anti-duplicado
          const checkDup = (list) => list.some(x => {
            const a = toCard(x);
            return a.title.toLowerCase()===title.toLowerCase() && (a.artist||'').toLowerCase()===(artist||'').toLowerCase();
          });
          const dupRemote = useRemote && remoteItems && checkDup(remoteItems);
          const dupLocal = !useRemote && checkDup(loadLocal());
          if(dupRemote || dupLocal){
            $hint.textContent = '¡Esa canción ya está en el muro! Prueba con otra ♪';
            $hint.className = 'song-hint error';
            return;
          }

          $hint.textContent = 'Guardando…';
          $hint.className = 'song-hint';

          if(useRemote){
            const inserted = await insertRemote(it);
            if(inserted){
              await fetchRemote();
              render();
              form.reset();
              $title.focus();
              $hint.textContent = '¡Gracias! Tu canción ya es visible para todos los invitados ♪';
              $hint.className = 'song-hint ok';
            } else {
              // fallback local si falla el remoto
              const items = loadLocal();
              items.unshift(it);
              saveLocal(items);
              render();
              form.reset();
              $hint.textContent = 'Se guardó en este dispositivo (el muro público no respondió, pero tu sugerencia quedó).';
              $hint.className = 'song-hint ok';
            }
          } else {
            // aún sin tabla → local + intenta remoto en background
            const items = loadLocal();
            items.unshift(it);
            saveLocal(items);
            insertRemote(it).then(()=> fetchRemote().then(()=> render()));
            render();
            form.reset();
            $title.focus();
            $hint.textContent = '¡Gracias! Tu canción se agregó al muro. Será pública cuando activemos la tabla.';
            $hint.className = 'song-hint ok';
          }
        });

        // init: intenta modo público
        fetchRemote().then(()=> render());
        // fallback render inmediato para no ver vacío
        render();
      })();
    </script>
  </body>
</html>

<style is:global>
@import url('https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500&family=Parisienne&family=Playfair+Display:ital,wght@0,400;0,500;1,400;1,500&display=swap');
:root{--cream:#F8F3EA;--off-white:#EDE9E2;--sand:#DCCDB4;--rose:#C9A496;--terracotta:#B07B5C;--brown:#8C6B58;--olive:#96702B;--taupe:#87736C;--espresso:#4C2D1F;--black:#1A1A1A;--white:#FFFFFF;--serif:'Playfair Display',Georgia,serif;--sans:'DM Sans',sans-serif}
*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:var(--cream);color:#3B2E24;font:17px/1.65 var(--sans)}main{overflow:hidden}
.cover{min-height:100svh;position:relative;display:grid;place-items:end center;padding-bottom:7vh;text-align:center;color:#F8F3EA;background:var(--espresso)}
.cover-art{position:absolute;inset:0;background:radial-gradient(120% 90% at 50% 0%,#6a4030 0%,#4C2D1F 55%,#33201a 100%)}
.cover-art:after{content:'';position:absolute;inset:0;background:radial-gradient(60% 45% at 50% 82%,#B07B5C33 0%,transparent 70%)}
.cover:after{content:'';position:absolute;inset:0;background:linear-gradient(180deg,#4C2D1F00,#4C2D1Fc7)}
.cover-copy{position:relative;z-index:1;width:min(92%,650px)}
.eyebrow,.label{letter-spacing:.18em;text-transform:uppercase;font-size:.7rem}
.cover .eyebrow{color:#E8DCC9}.small-title{font:italic 1.6rem var(--serif);margin:4rem 0 0}
.cover h1{font:clamp(10rem,35vw,19rem)/.8 var(--serif);font-weight:400;letter-spacing:-.08em;margin:.4rem 0;background:linear-gradient(110deg,#FFF7EC 12%,#DCCDB4 35%,#FFF8EE 48%,#B07B5C 62%,#EAD9C2 80%);background-size:220% 100%;-webkit-background-clip:text;background-clip:text;color:transparent;text-shadow:none;animation:xv-shine 4.5s ease-in-out infinite}
@keyframes xv-shine{0%,100%{background-position:0% 50%}50%{background-position:100% 50%}}
.name{font:italic clamp(4.5rem,16vw,10rem)/.85 'Parisienne',cursive;letter-spacing:.01em;text-transform:none;margin:3.5rem 0 1.15rem;color:#F6ECD9;text-shadow:0 2px 18px #2a160dcc}
.date{margin-top:1.6rem;letter-spacing:.2em;color:#E8DCC9}
.scroll-arrow{display:inline-block;margin-top:5rem;color:#DCCDB4;text-decoration:none;font:2rem/1 var(--serif);animation:float-arrow 2s ease-in-out infinite}
.scroll-arrow:focus-visible{outline:1px solid #fff;padding:.3rem}
@keyframes float-arrow{50%{transform:translateY(7px)}}
.section{padding:clamp(5rem,12vw,10rem) clamp(1.5rem,8vw,9rem)}
.eyebrow{color:var(--olive);margin:0 0 .8rem}
.story{background:var(--cream);text-align:center;position:relative}
.invite{margin-bottom:clamp(2.5rem,6vw,4rem)}
.invite-family{font:italic 1.15rem var(--serif);margin:0 0 .5rem;letter-spacing:.02em}
.invite-cta{margin:.4rem 0;color:#6B564A}
.invite-hero{font:italic clamp(3rem,10vw,6rem)/1 'Parisienne',cursive;margin:.4rem 0 0;color:var(--espresso)}
.story-heading{font:400 clamp(2.4rem,6vw,4.2rem)/1.08 var(--serif);letter-spacing:-.03em;margin:0 0 2rem}
.story-heading em,.details-title em{font-style:italic;color:var(--terracotta);background:linear-gradient(110deg,#8C6B58,#DCCDB4 60%,#96702B);-webkit-background-clip:text;background-clip:text;color:transparent}
.copy{max-width:620px;margin:0 auto;text-align:left}
.invite-copy p{margin:0 0 1.1rem;color:#4C3B32}
.signature{font:italic 1.3rem var(--serif);color:var(--espresso);margin-top:1.6rem}
.event{position:relative;overflow:hidden;background:var(--espresso);color:#F3E9DC;text-align:center}
.event .eyebrow{color:var(--sand)}
.event-card{display:flex;justify-content:center;gap:clamp(2.5rem,8vw,6rem);margin:1rem 0 2.5rem}
.event-card .label{color:#CBB8A2;display:block;margin-bottom:.5rem}
.event-card strong{font:400 clamp(2rem,5vw,3.2rem)/1 var(--serif)}
.date-display{display:flex;align-items:center;gap:.7rem;justify-content:center}
.date-display b{font-size:clamp(3.4rem,9vw,5.6rem);line-height:.9;color:var(--sand)}
.date-display span{font-size:1.05rem;line-height:1.25;text-align:left;color:#F3E9DC}
.date-display small{color:#CBB8A2}
.countdown{display:flex;justify-content:center;gap:clamp(1rem,4vw,2.2rem);margin:0 0 2rem}
.countdown div{min-width:74px}
.countdown b{font:500 clamp(1.6rem,4vw,2.4rem) var(--sans);color:var(--sand)}
.countdown span{display:block;font:500 .66rem var(--sans);letter-spacing:.16em;text-transform:uppercase;color:#CBB8A2}
.punctual{max-width:480px;margin:0 auto;color:#D9CBB8;font-size:.95rem}
.venue{position:relative;overflow:hidden;display:grid;grid-template-columns:1fr 1fr;gap:clamp(1.5rem,5vw,4rem);align-items:center;background:var(--cream)}
.venue-photo{position:relative;min-height:320px;border-radius:2px;overflow:hidden;box-shadow:14px 14px 0 var(--sand)}
.venue-photo iframe{position:absolute;inset:0;width:100%;height:100%}
.venue-copy .eyebrow{margin-top:0}
.venue-copy h2{font:400 clamp(2rem,5vw,3.2rem)/1.1 var(--serif);letter-spacing:-.02em;margin:.2rem 0 1rem;color:var(--espresso)}
.venue-copy h2 em{color:var(--terracotta)}
.venue-copy address{font-style:normal;color:#4C3B32;margin:0 0 1.4rem}
.button{display:inline-block;border:1px solid var(--terracotta);background:var(--terracotta);color:#FFF8EE;text-decoration:none;padding:.85rem 1.5rem;font:500 .78rem var(--sans);letter-spacing:.1em;text-transform:uppercase}
.button:hover{background:var(--brown);border-color:var(--brown)}
.button span{display:inline-block;margin-left:.3rem}
.details{position:relative;overflow:hidden;text-align:center}
.details-title{font:400 clamp(2.2rem,5vw,3.6rem)/1.1 var(--serif);letter-spacing:-.03em;margin:0 0 2.5rem;color:var(--espresso)}
.detail-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:1.4rem;max-width:760px;margin:0 auto}
.detail-grid article{border:1px solid #E0D6C2;background:#FBF6EC;padding:1.8rem 1.4rem;box-shadow:8px 8px 0 #DCCDB4}
.detail-symbol{display:inline-block;font:1.5rem var(--serif);color:var(--terracotta);margin-bottom:.6rem}
.detail-grid h3{font:400 1.35rem var(--serif);margin:.2rem 0 .9rem;color:var(--espresso)}
.detail-grid h3 em{color:var(--terracotta)}
.detail-grid p{margin:0;color:#4C3B32;font-size:.95rem}
.earth-word{color:var(--olive);font-weight:500}
.rsvp{position:relative;overflow:hidden;background:var(--espresso);color:#F3E9DC;text-align:center}
.rsvp .eyebrow{color:var(--sand)}
.rsvp h2{font:400 clamp(2.4rem,6vw,4rem)/1 var(--serif);letter-spacing:-.03em;margin:0 0 1.2rem}
.rsvp h2 em{color:var(--sand)}
.rsvp p{color:#D9CBB8;margin:0 0 .6rem}
.actions{margin-top:1.4rem}
.button-light{background:transparent;color:var(--sand);border-color:var(--terracotta)}
.button-light:hover{background:var(--terracotta);color:#FFF8EE}
footer{background:#2E1C14;color:#B39A80;text-align:center;padding:3rem 1rem 2.5rem;position:relative;font-size:.9rem}
.hashtag{letter-spacing:.08em;margin:0 0 .4rem}
.footer-name{font:italic 1.2rem var(--serif);color:#E8DCC9;margin:0}
/* ── Canciones: muro de sugerencias ── */
.songs{background:var(--cream);text-align:center;position:relative;overflow:hidden}
.songs:before{content:'♪';position:absolute;top:2.2rem;right:6%;font:3.2rem var(--serif);color:var(--sand);opacity:.18;pointer-events:none;aria-hidden:true;transform:rotate(12deg)}
.songs h2{font:400 clamp(2.2rem,5vw,3.6rem)/1.1 var(--serif);letter-spacing:-.03em;margin:0 0 .8rem;color:var(--espresso)}
.songs h2 em{font-style:italic;color:var(--terracotta);background:linear-gradient(110deg,#8C6B58,#DCCDB4 60%,#96702B);-webkit-background-clip:text;background-clip:text;color:transparent}
.songs-intro{max-width:560px;margin:0 auto 2rem;color:#4C3B32;font-size:1rem}
.song-form{max-width:720px;margin:0 auto 2.2rem;text-align:left;background:#FBF6EC;border:1px solid #E0D6C2;box-shadow:8px 8px 0 #DCCDB4;padding:1.6rem 1.4rem}
.song-grid{display:grid;grid-template-columns:1fr 1fr 1fr;gap:1rem;margin-bottom:1rem}
.field{display:flex;flex-direction:column;gap:.35rem}
.field span{font:500 .72rem var(--sans);letter-spacing:.08em;text-transform:uppercase;color:var(--olive)}
.field span i{font-style:normal;text-transform:none;letter-spacing:0;color:#8a7a6e;font-weight:400}
.field input,.field textarea{width:100%;border:1px solid #DCCDB4;background:#fff;color:#3B2E24;font:400 .95rem var(--sans);padding:.75rem .9rem;border-radius:2px;outline:none;transition:border-color .18s,box-shadow .18s}
.field input:focus,.field textarea:focus{border-color:var(--terracotta);box-shadow:0 0 0 3px #B07B5C22}
.field textarea{resize:vertical;min-height:56px}
.field-full{margin-bottom:1rem}
.song-actions{display:flex;align-items:center;gap:1rem;flex-wrap:wrap}
.song-actions .button{min-height:44px;cursor:pointer}
.song-hint{margin:0;font-size:.88rem;color:var(--olive);min-height:1.2em}
.song-hint.error{color:#9c4a2e}
.song-hint.ok{color:#5a7247}
.song-wall-wrap{max-width:720px;margin:0 auto;text-align:left}
.song-wall-head{display:flex;align-items:baseline;justify-content:space-between;margin-bottom:.8rem}
.song-wall-head h3{font:500 1.05rem var(--serif);color:var(--espresso);margin:0}
.song-count{background:var(--espresso);color:var(--sand);font:700 .78rem var(--sans);padding:.2rem .55rem;border-radius:999px;min-width:1.6rem;text-align:center}
.song-wall{list-style:none;margin:0;padding:0;display:grid;gap:.9rem}
.song-card{background:#fff;border:1px solid #E8D9C2;padding:1rem 1.1rem;display:grid;gap:.35rem;position:relative;overflow:hidden}
.song-card:before{content:'';position:absolute;left:0;top:0;bottom:0;width:3px;background:linear-gradient(180deg,var(--terracotta),var(--sand))}
.song-card-head{display:flex;align-items:flex-start;justify-content:space-between;gap:.8rem}
.song-card-title{font:500 1.05rem var(--serif);color:var(--espresso);margin:0;line-height:1.2}
.song-card-artist{font:400 .88rem var(--sans);color:#6B564A;margin:0}
.song-card-meta{font:500 .68rem var(--sans);letter-spacing:.1em;text-transform:uppercase;color:var(--olive);white-space:nowrap}
.song-card-comment{margin:0;color:#4C3B32;font-size:.92rem;line-height:1.5;background:#F8F3EA;padding:.6rem .7rem;border-radius:2px}
.song-card-actions{display:flex;gap:.6rem;margin-top:.2rem}
.song-card-actions button{border:1px solid #E0D6C2;background:#FBF6EC;color:var(--espresso);font:500 .72rem var(--sans);letter-spacing:.06em;text-transform:uppercase;padding:.4rem .65rem;cursor:pointer}
.song-card-actions button:hover{background:var(--sand)}
.song-empty{margin:1rem 0 0;color:#8a7a6e;font-size:.92rem;text-align:center;background:#FBF6EC;border:1px dashed #DCCDB4;padding:1.2rem}
.song-card{animation:song-in .32s ease}
@keyframes song-in{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:none}}
/* ── Música FAB ── */
.music-fab{position:fixed;right:16px;bottom:16px;z-index:30;display:inline-flex;align-items:center;gap:.5rem;padding:.62rem .9rem;border:1px solid #E0D6C2;background:#FBF6EC;color:var(--espresso);font:500 .78rem var(--sans);letter-spacing:.08em;text-transform:uppercase;box-shadow:6px 6px 0 #DCCDB4;cursor:pointer;min-height:44px}
.music-fab:hover{background:var(--sand)}
.music-fab[aria-pressed="true"]{background:var(--espresso);color:var(--sand);border-color:var(--espresso)}
.music-fab:focus-visible{outline:2px solid var(--terracotta);outline-offset:2px}
.music-icon{font:1.1rem var(--serif);line-height:1}
@media (prefers-reduced-motion: reduce){ .music-fab{transition:none} }
@media (max-width:720px){ .music-fab{right:12px;bottom:12px;padding:.55rem .75rem} }
@media (max-width:720px){
  .venue{grid-template-columns:1fr}
  .venue-photo{min-height:240px}
  .cover-copy{width:min(94%,520px)}
  .song-grid{grid-template-columns:1fr}
  .song-form{padding:1.2rem 1rem}
  .songs:before{top:1.2rem;right:4%;font-size:2.4rem}
}
</style>
